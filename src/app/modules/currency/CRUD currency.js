CRUD currency

lista de registro (nombre del modulo)

button a la izquierda superior '+'  para agregar

cada row tiene bottones de gestion ('+', 'x') que solo se muestran en hover

seleccion multiple

campos del model (name, abreviation, image)

envio de imagen base64


hover en list verdde


Resumen de actividad al final del dia 15/11/17

quede mostrandoe la botonera de las opciones de la tabla de currrencies
esta comentado el if porque hay errores de sintaxis
*ngIf="{{showButton[currency.index].show == true}}"
instruccion que no funciona

